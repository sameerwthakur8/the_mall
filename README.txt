Installation of the Flutter requires installation of following programs.
    - Android Studio
    - XCODE if using Mac
    - Device plugged on to the personal computer for deploying the application

The installation guide for flutter can be found at "https://flutter.dev/docs/get-started/install".

The pubspec.yaml contains the dependencies needed for running the widgets, fonts, icons and assets such as images,